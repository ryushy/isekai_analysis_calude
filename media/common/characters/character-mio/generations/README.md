# generations/

澪のキャラシート候補を複数回生成して残す場所です。

```text
generations/
└── run-YYYYMMDD_HHMMSS/
    ├── prompt.md
    ├── candidates/
    ├── review.md
    └── selected/
```

良かった候補だけを `outputs/ref_sheet_v*.png` に昇格します。
